<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Movies</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body class="pastel-body">

<nav class="navbar navbar-expand-lg pastel-navbar shadow-sm">
    <div class="container">
        <a class="navbar-brand pastel-brand" href="/">🌸 Movie Watchlist</a>
        <ul class="navbar-nav ms-auto">
            <li class="nav-item"><a class="nav-link pastel-link" href="/">Home</a></li>
            <li class="nav-item"><a class="nav-link pastel-link" href="/movies">Movies</a></li>
        </ul>
    </div>
</nav>

<div class="container mt-4">

    <h1 class="page-title mb-3">All Movies 🎬</h1>

    <!-- Add Movie Button -->
    <a href="/movies/new" class="btn btn-pastel-add mb-3">➕ Add Movie</a>

    <div class="list-group shadow-sm">

        <% movies.forEach(movie => { %>
            <div class="list-group-item d-flex justify-content-between align-items-center">

                <div>
                    <strong><%= movie.title %></strong> - <%= movie.genre %> 
                    (<%= movie.year %>)
                    <span class="badge-status"><%= movie.status %></span>
                </div>

                <div>
                    <a href="/movies/<%= movie._id %>/edit" class="btn btn-warning btn-sm">Edit</a>
                    <form action="/movies/<%= movie._id %>?_method=DELETE" 
                          method="POST" style="display:inline;">
                        <button class="btn btn-danger btn-sm">Delete</button>
                    </form>
                </div>

            </div>
        <% }) %>

    </div>

</div>

<footer class="text-center pastel-footer">
    © 2025 Movie Watchlist
</footer>

</body>
</html>
