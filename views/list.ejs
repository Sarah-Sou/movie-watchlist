<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>All Movies</title>
<link rel="stylesheet" href="/css/style.css">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="pastel-body">
<nav class="navbar pastel-navbar shadow-sm">
    <a class="navbar-brand pastel-brand" href="/">🌸 Movie Watchlist</a>
    <ul class="navbar-nav ms-auto">
        <li class="nav-item"><a class="nav-link pastel-link" href="/movies">Movies</a></li>
        <li class="nav-item">
            <form action="/logout" method="POST"><button class="btn btn-logout">Logout</button></form>
        </li>
    </ul>
</nav>
<div class="container mt-4">
<h1 class="page-title">All Movies 🎬</h1>
<a href="/movies/new" class="btn btn-add">Add Movie</a>
<ul class="list-group mt-3">
<% movies.forEach(movie => { %>
<li class="list-group-item d-flex justify-content-between align-items-center">
<div>
<strong><%= movie.title %></strong> - <%= movie.genre %> (<%= movie.year %>)
<span class="badge status-badge"><%= movie.status %></span>
</div>
<div>
<a href="/movies/<%= movie._id %>/edit" class="btn btn-warning btn-sm">Edit</a>
<form action="/movies/<%= movie._id %>?_method=DELETE" method="POST" style="display:inline;">
<button class="btn btn-danger btn-sm">Delete</button>
</form>
</div>
</li>
<% }) %>
</ul>
</div>
</body>
</html>
